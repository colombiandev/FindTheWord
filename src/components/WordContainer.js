import Word from './Word';

const WordContainer = (props) => {

    return (
        <div>
            WordContainer
            <Word />
            <Word />
            <Word />
        </div>
    );
}

export default WordContainer;